# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: patrick's blog
email: pleasedont@email.me
description: >- # this means to ignore newlines until "baseurl:"
  Patrick Thill's blog.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://thill.me" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: kpthill
github_username:  kpthill
show_excerpts: true

# Build settings
theme: minima
plugins:
  - jekyll-feed

# Suppress Dart Sass deprecation warnings that come from the minima gem.
# These are not our code to fix; remove this once minima releases a Sass 3.0-compatible version.
sass:
  quiet_deps: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Submodule blacklist: serve the whole dodo submodule except private/unwanted files.
# We'd prefer a whitelist, but GitHub Pages uses Jekyll 3.x where `include:` does
# not override an explicit `exclude:` entry, so a blacklist is the only option.
# Note: new files added to the dodo repo will be served publicly by default.
exclude:
  - AGENTS.md
  - dodo/AGENTS.md
  - dodo/CLAUDE.md
  - dodo/README.md
  - dodo/dodo-spec.md
  - dodo/dodo.js
  - dodo/ex2.dodo
  - dodo/examples
  - dodo/impl
  - dodo/implementation-notes.md
  - dodo/notes_to_self.md
  - dodo/package-lock.json
  - dodo/package.json
  - dodo/test
  - dodo/todo.md
