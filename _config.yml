# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: patrick's blog
email: pleasedont@email.me
description: >- # this means to ignore newlines until "baseurl:"
  Patrick Thill's blog.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://thill.me" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: kpthill
github_username:  kpthill
show_excerpts: true

# Recurse Center webring (https://ring.recurse.com/) membership. The header
# dropdown renders the required links (and head.html loads the ring.js
# rewriter) only when the id is set.
recurse_webring_id: 87
recurse_webring_uuid: 2ddcccb0-01de-4e9a-b93a-4a098aba74c8

# Build settings
theme: minima
plugins:
  - jekyll-feed

# Suppress Dart Sass deprecation warnings that come from the minima gem.
# These are not our code to fix; remove this once minima releases a Sass 3.0-compatible version.
sass:
  quiet_deps: true

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# The dodo pages (thill.me/dodo/) are no longer served from this repo: the
# dodo repo deploys its own GitHub Pages project site, which takes precedence
# over this user site for the /dodo/ path. See AGENTS.md for the pattern.
exclude:
  - AGENTS.md
